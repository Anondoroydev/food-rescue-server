generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Add your models here. Empty schema is valid for generating client.
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
